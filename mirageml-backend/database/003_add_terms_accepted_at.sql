-- Добавляем поле для хранения даты принятия пользовательского соглашения
ALTER TABLE users ADD COLUMN IF NOT EXISTS terms_accepted_at TIMESTAMP WITH TIME ZONE;

-- Комментарий
-- Это поле будет содержать дату и время, когда пользователь принял условия
-- Для существующих пользователей поле будет NULL (они ещё не принимали)
